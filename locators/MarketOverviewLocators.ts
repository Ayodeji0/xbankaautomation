export const MarketOverviewLocators = {

    // Navigation
    marketOverviewMenu: "role=link[name='Market Overview']",

    // Page
    marketHeading: "role=heading[name='Market']",

    // Market Tabs
    favoritesTab: "role=button[name='Favorites']",
    spotTab: "role=button[name='Spot']",
    futuresTab: "role=button[name='Futures']",

    // Filters
    allButton: "role=button[name='All']",
    newButton: "role=button[name='New']",

    // Search
    searchCoinField: "role=textbox[name='Search coin']",

    // Actions
    tradeButton: "role=link[name='Trade']",

};